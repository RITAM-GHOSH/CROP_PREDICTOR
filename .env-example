# Environment variables for Crop & Fertilizer Recommendation System
# Rename this file to .env and update the values with your credentials

# Email Configuration
EMAIL_USER=your-email@example.com
EMAIL_PASSWORD=your-password-or-app-password
SMTP_SERVER=smtp.gmail.com
SMTP_PORT=587

# Optional: Set to 'True' to enable debug mode
DEBUG=False